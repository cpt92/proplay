# Frontend (Vite exposes VITE_* vars to the browser)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Server (only available in serverless functions, never exposed to the browser)
STRIPE_SECRET_KEY=sk_test_...
